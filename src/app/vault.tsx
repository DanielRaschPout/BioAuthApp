/**
 * src/app/vault.tsx — Route: "/vault"
 *
 * Expo Router maps this file to the "vault" tab. Its only job is to re-export
 * the screen component, keeping the routing layer thin and the screen logic
 * completely independent of the navigation system.
 */

import SecureVaultScreen from '@/screens/SecureVaultScreen';

export default SecureVaultScreen;
