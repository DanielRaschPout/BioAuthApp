/**
 * src/app/index.tsx — Route: "/"
 *
 * In Expo Router, every file inside `src/app/` maps to a URL/route. This
 * file maps to the root tab ("/index"). Its only job is to render the full-
 * page screen component — keeping routing concerns separate from UI logic.
 */

import LoginScreen from '@/screens/LoginScreen';

export default LoginScreen;
